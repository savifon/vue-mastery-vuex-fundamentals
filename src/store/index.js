// import { reactive } from 'vue'

// export default reactive({ flashMessage: '', event: null })

import { createStore } from 'vuex'

export default createStore({
  state: { user: 'Adam Jahr' },
  mutations: {},
  actions: {},
  modules: {},
})
